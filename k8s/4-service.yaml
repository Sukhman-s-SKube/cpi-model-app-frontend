apiVersion: v1
kind: Service
metadata:
  name: cpi-model-app-frontend
  namespace: cpi-model-app
spec:
  type: LoadBalancer
  selector: { app: cpi-model-app-frontend }
  ports: [ { port: 80, targetPort: 80 } ]